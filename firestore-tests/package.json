{
  "name": "greenaccess-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Tests des Firestore Security Rules (greenaccess-16d25) contre l'émulateur Firestore, via @firebase/rules-unit-testing.",
  "scripts": {
    "test": "node --test rules.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4"
  }
}
